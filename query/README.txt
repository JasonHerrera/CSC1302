This project is a functional relational database with sample data files and sample queries for the database.

To run the queries, add the company directory in the command line as an argument:

Ex. $ java Query1 company
